{
  "nbformat": 4,
  "nbformat_minor": 0,
  "metadata": {
    "colab": {
      "provenance": [],
      "authorship_tag": "ABX9TyM3vjEkHNFmzwU2kYqCAFiu",
      "include_colab_link": true
    },
    "kernelspec": {
      "name": "python3",
      "display_name": "Python 3"
    },
    "language_info": {
      "name": "python"
    }
  },
  "cells": [
    {
      "cell_type": "markdown",
      "metadata": {
        "id": "view-in-github",
        "colab_type": "text"
      },
      "source": [
        "<a href=\"https://colab.research.google.com/github/TianaT6/MSSP607/blob/main/week3\" target=\"_parent\"><img src=\"https://colab.research.google.com/assets/colab-badge.svg\" alt=\"Open In Colab\"/></a>"
      ]
    },
    {
      "cell_type": "markdown",
      "source": [
        "a.The code in a function executes when the function is called."
      ],
      "metadata": {
        "id": "_D-Pwq9Ko9IW"
      }
    },
    {
      "cell_type": "markdown",
      "source": [
        "b.There is one global scope. A new local scope is created each time a function is called."
      ],
      "metadata": {
        "id": "1vUYXF8MpHvu"
      }
    },
    {
      "cell_type": "markdown",
      "source": [
        "c.The local variables are removed after the function finishes."
      ],
      "metadata": {
        "id": "64Bm3jako6YD"
      }
    },
    {
      "cell_type": "markdown",
      "source": [
        "d.We can use `try` and `except` to handle errors and prevent the program from crashing."
      ],
      "metadata": {
        "id": "c7Q4GhonpOtV"
      }
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {
        "colab": {
          "base_uri": "https://localhost:8080/"
        },
        "id": "xroMJXaUn_Ge",
        "outputId": "54030dda-8398-48be-c916-f5503e08f2d8"
      },
      "outputs": [
        {
          "output_type": "stream",
          "name": "stdout",
          "text": [
            "Enter a number: 11\n",
            "34 17 52 26 13 40 20 10 5 16 8 4 2 1 "
          ]
        }
      ],
      "source": [
        "def collatz(number):\n",
        "    if number % 2 == 0:\n",
        "        result = number // 2\n",
        "    else:\n",
        "        result = 3 * number + 1\n",
        "    print(result, end=' ')\n",
        "    return result\n",
        "number = int(input(\"Enter a number: \"))\n",
        "while number != 1:\n",
        "    number = collatz(number)"
      ]
    }
  ]
}